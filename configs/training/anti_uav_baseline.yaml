# Small reproducible baseline for the modern Anti-UAV detector path.
# Supply the prepared dataset with: --data /path/to/drone.yaml

model: yolo11n.pt
epochs: 100
imgsz: 640
batch: 16
device: null
workers: 2
project: runs/train
name: anti-uav-baseline
